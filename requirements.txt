# Core
alpaca-py>=0.30.0
yfinance>=0.2.0
edgartools>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
asyncpg>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
aiofiles>=23.0.0

# Logging
structlog>=24.1.0

# Market calendar
exchange_calendars>=4.5.0

# / LLM providers are accessed via httpx (see src/data/llm_client.py) — no SDKs required

# Dashboard
fastapi>=0.100.0
uvicorn>=0.22.0

# ML (hmmlearn + arch may be culled in phase 4 step 3 if hmm_regime/garch_nn are removed)
lightgbm>=4.0.0
hmmlearn>=0.3.0
arch>=6.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.4.0
